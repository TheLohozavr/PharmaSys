extends ../../layout

block content
  h2.page-title Сотрудники
  div(style="margin-bottom:16px")
    a.btn.btn-primary(href="/admin/employees/new") + Добавить сотрудника
  .card
    .table-wrap
      table
        thead
          tr
            th ФИО
            th Логин
            th Роль
            th Телефон
            th Статус
            th
        tbody
          each e in employees
            tr
              td= e.full_name
              td= e.login
              td= e.role_name
              td= e.phone || '—'
              td= e.is_active ? 'Активен' : 'Отключён'
              td
                form(action=`/admin/employees/${e.id}/toggle` method="POST" style="display:inline")
                  button.btn.btn-sm(class=e.is_active ? 'btn-warning' : 'btn-secondary' type="submit")
                    = e.is_active ? 'Отключить' : 'Включить'
